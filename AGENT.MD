# Quiet Tree 项目协作约定

## 开始工作前：确认唯一正式源码

- 产品名 Quiet Tree，插件 ID `quiet-tree`，许可证 MIT。
- 正式仓库：<https://github.com/elfmedy/quiet-tree>。当前开发机检出目录是 `D:/work/quiet-tree`。
- `D:/work/flex_explorer` 是早期网页交互原型；其中 `obsidian-plugin/` 是历史开发副本，**不是正式插件源码或发布来源**。两个目录不会自动同步。不要从该副本整包覆盖正式仓库。
- 即使任务工作目录是 `flex_explorer`，插件修复、构建、测试、提交和发布也必须在正式仓库进行。网页原型任务才使用原型目录。
- 先检查 `git rev-parse --show-toplevel`、`git remote -v`、`git status --short`、当前提交、`manifest.json` 和 `package.json`；需要远端信息时 `git fetch origin`，核对远端 main、标签和最新 Release。路径名和旧对话里的版本号不能证明源码是最新的。
- 发现分歧时保留已有未提交改动，按功能迁移、审查差异；不得用旧版代码、数据模型或构建文件覆盖新版。需要新分支时使用 `codex/` 前缀。
- 同时有其他任务修改插件或使用 Obsidian 时，检查其工作状态和目标库，避免覆盖修改或重启整个 Obsidian 进程。

## 已确认的产品与交互

- 聚焦增强 Obsidian 原生文件树的手动排序；可以跨目录移动，移动通过 FileManager 完成以更新链接。新功能以用户本次范围为准，不自动扩展历史调研中的 P1/P2 项。
- 文件树在拖动时保持稳定，不插入大块占位空白。使用横向插入线、空心起点和缩进表达落点；目录边界有歧义时用选择框明确父目录及相邻位置。
- 竖向拖动引导线和按下期间的动态进度横线已移除，不因旧原型曾经存在而恢复。
- 普通原位拖动减少提示；目录边界选择即使保持原位，也显示当前选项的插入线，并说明“保持当前位置”。
- 鼠标默认延迟 200 ms，触屏默认 500 ms，独立设置且保留自定义值；普通点击与提前滑动不能误触拖动。
- 整行模式不显示手柄，手柄模式才显示。拖动反馈只影响当前列表和拖动期间，取消后恢复原生状态。
- 桌面提起后显示 Esc 取消提示；触屏显示底部取消区，进入后停止自动滚动，松手取消。选择框不得遮挡取消提示。
- 中英文界面，通用/语言设置置顶，说明简洁并按需展开。保留移动端原生布局、延迟加载的文件列表和设置搜索支持。
- **自 0.4.2 起完全移除按其他插件 ID 判断冲突并暂停排序的功能。** 不扫描 `enabledPlugins` 来阻止 Quiet Tree；残留 ID 不等于已安装或运行。保留实际接口能力检查、异常降级和只恢复自己挂钩的卸载行为。

## 数据与同步约定

- 排序与设置存入插件自己的 `data.json`，顶层 `dataVersion: 1` 与发布版本独立；排序以单个数组字段 `orderState` 保存。以 `src/data.ts` 为准。
- 不重新引入可配置独立排序文件或文件轮询。外部设置变化使用现有事件入口加载；布局检查定时器不是排序文件轮询。
- 保留串行读写队列、读取最新同步数据、失败时保留最后有效状态、移动保存失败后的回滚。
- 已知旧格式先备份再迁移；迁移完成后数据独立，不继续引用 Flexplorer 或旧排序文件。
- 损坏数据不自动清空，使用已有明确确认的备份重置入口；高版本数据不覆盖、不重置；旧文件缺失时等待同步或恢复，不用空数据覆盖。
- 排除规则按目录边界匹配。现有 `assets` 规则需保留：排除目录内部排序不意味着移除该目录在父目录中的位置。
- 不因启动时暂时未加载文件而删除排序记录。不公开上传任何真实库的 `data.json`、排序文件、笔记或备份。
- 数据格式升级、性能优化和兼容性修复均不得重置已有排序。

## 开发与验证

- 主要代码：`src/drag.ts`（交互及测量缓存）、`src/native.ts`（原生接口）、`src/main.ts`（生命周期及事件）、`src/data.ts`（版本化数据）、`lib/`（树模型及落点）。
- 保留布局缓存、增量行装饰、批量文件事件合并。缓存需响应滚动、尺寸、主题、折叠和虚拟行替换，提交落点前重新校验。
- DOM/观察器/事件使用文件树所属窗口；不要假定全局 document 就是当前文件树的 document。
- 开发运行时按 `package.json` 要求使用 Node 22.13+。依赖变更时使用锁文件；标准检查：`npm run lint`、`npm run typecheck`、`npm test`、`npm run build`，每项必须核对退出状态。
- Obsidian 集成测试默认使用隔离的 Obsidian Sandbox。测试脚本先校验库名称；创建唯一测试目录，避免覆盖既有夹具；完成后清理本次数据、恢复临时设置。
- 用户日常 work 库不是测试库。发布后优先通过社区市场更新；只有明确要求手动部署时才复制构建文件到日常库。Sandbox 可以手动部署测试构建。
- 单元测试、桌面合成触屏事件、移动布局模拟、真实 iPhone/Android 测试分别报告，不把模拟测试称为真机验证。最低支持版本以 manifest 为准，未实测时说明验证缺口。

## 发布

1. 区分用户要求：仅讨论不修改；仅上传则提交/推送且不打标签；明确发布 Release 才完成发布流程。已授权的发布无需重复确认。
2. 在正式仓库同步 `package.json`、`package-lock.json`（含根 package 条目）、`manifest.json` 和 `versions.json`，补充 CHANGELOG 与 `.github/release-notes/<版本>.md`。补丁修复不随意提高数据格式或最低 Obsidian 版本。
3. 完成检查及相关回归，审查待提交文件，确保没有库数据、临时输出或原型工程。只提交本次相关修改。
4. 推送提交；创建与 manifest 一致、无 `v` 前缀且未使用过的版本标签，并推送。`.github/workflows/release.yml` 自动检查、构建并发布三个安装附件。
5. 等待 GitHub Actions 的 build/release 成功，确认 Release 非草稿、版本正确，`main.js`、`manifest.json`、`styles.css` 可下载，核对主构建摘要。验证后提供 Release 链接。
6. GitHub main、最新 Release、Sandbox 安装版与用户设备安装版是四个不同状态；分别描述，不能因为推送源码就声称设备已更新。

## 维护本约定

本文件根据原型开发、安装/同步/交互改进、P0 优化与发布三个项目对话整理于 2026-09-18。较新的明确决定优先，例如 0.4.0 的数据迁移取代早期“不兼容旧格式”的决定，0.4.2 的移除冲突检测取代 0.4.1 的自动暂停策略。用户改变约定后及时更新此文件，版本状态始终重新查询。
